{
  "name": "agentic-management-gas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "push": "clasp push -f",
    "deploy": "node scripts/deploy.mjs",
    "coverage": "vitest run --coverage --coverage.include='src/**' --coverage.extension='.js' --coverage.extension='.gs' --coverage.reporter=text"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "jsdom": "^30.0.1",
    "vitest": "^5.0.0"
  }
}
